import { Tabs } from "nextra/components";

### Get Token Balance

Retrieves the current token balance for a user.

- **URL**:`/api/tokens/balance`
- **Method**:`GET`
- **Headers**:
  - `X-Developer-API-Key`: Your Developer API Key
  - `X-User-Connection-Key`: User Connection Key

#### Response

```json
{
  "balance": number
}
```

#### Error Response

```json
{
  "error": string
}
```

```typescript filename="get-balance.ts" {3} copy
const response = await fetch("https://api.hiero.com/api/tokens/balance", {
	method: "GET",
	headers: {
		"X-Developer-API-Key": "YOUR_DEVELOPER_API_KEY",
		"X-User-Connection-Key": "USER_CONNECTION_KEY",
	},
});

const data = await response.json();
```

#### Example

<Tabs items={['TypeScript', 'Python', 'Curl']}>
<Tabs.Tab>
  This is the TypeScript example.

```ts filename="get-balance.ts" {3} copy
const response = await fetch("https://api.hiero.com/api/tokens/balance", {
	method: "GET",
	headers: {
		"X-Developer-API-Key": "YOUR_DEVELOPER_API_KEY",
		"X-User-Connection-Key": "USER_CONNECTION_KEY",
	},
});

const data = await response.json();
```

End example

</Tabs.Tab>
<Tabs.Tab>
This is the Python example.

```py filename="get-balance.py" {3} copy
import requests

def get_balance():
  response = requests.get(
    "https://api.hiero.com/api/tokens/balance",
    headers={
      "X-Developer-API-Key": "YOUR_DEVELOPER_API_KEY",
      "X-User-Connection-Key": "USER_CONNECTION_KEY",
    },
  )
  data = response.json()
  return data
```

End example

</Tabs.Tab>

<Tabs.Tab>
This is the Curl example.
```bash filename="get-balance.sh" {3} copy
curl -X GET "https://api.hiero.com/api/tokens/balance" \
  -H "X-Developer-API-Key: YOUR_DEVELOPER_API_KEY" \
  -H "X-User-Connection-Key: USER_CONNECTION_KEY"
```
End example

</Tabs.Tab>
</Tabs>
